# Verified ASL reference assets

Place verified standard ASL alphabet reference visuals here using these exact names:

`A.webp` through `Z.webp`

The ASL Neural Library renders an explicit pending-asset state until each verified visual is available. J and Z remain labeled as dynamic gestures.
