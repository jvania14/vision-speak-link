import { createFileRoute, Link } from "@tanstack/react-router";
import { Stethoscope, Volume2 } from "lucide-react";
import { Button } from "@/components/ui/button";
import { CommunicationHistory, SectionLabel } from "@/components/ProductComponents";
import { useApp } from "@/context/AppContext";

export const Route=createFileRoute("/doctor")({head:()=>({meta:[{title:"Doctor Mode — Silent Talk"},{name:"description",content:"A clear healthcare staff view of the patient's current communication."},{property:"og:title",content:"Doctor Mode — Silent Talk"},{property:"og:description",content:"Review and speak the patient's current message and communication history."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:DoctorMode});
function DoctorMode(){const {message,history,speak}=useApp();return <div className="page-wrap"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><SectionLabel>Healthcare staff view</SectionLabel><h1 className="mt-3 font-display text-4xl font-semibold sm:text-5xl">Doctor mode</h1><p className="mt-3 text-muted-foreground">Patient communication, presented clearly for faster understanding.</p></div><Button asChild variant="outline"><Link to="/patient">Switch to Patient Mode</Link></Button></div><section className="panel mt-8 p-6 sm:p-10"><div className="flex items-center gap-3 text-primary"><Stethoscope/><SectionLabel>Patient communication</SectionLabel></div><p className="mt-8 text-sm text-muted-foreground">Current message</p><p className="mt-4 min-h-32 font-display text-4xl font-semibold leading-tight sm:text-6xl">“{message || "WAITING FOR PATIENT MESSAGE"}”</p><Button size="lg" className="mt-8" disabled={!message} onClick={()=>speak(message)}><Volume2/> Speak message</Button></section><div className="mt-6"><CommunicationHistory entries={history}/></div></div>}