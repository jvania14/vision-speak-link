import { createFileRoute } from "@tanstack/react-router";
import { motion } from "motion/react";
import { Camera, Hand, ScanLine, MessageSquareText, Volume2 } from "lucide-react";
import { PageIntro, } from "@/components/AppShell";
import { SectionLabel } from "@/components/ProductComponents";

export const Route=createFileRoute("/how-it-works")({head:()=>({meta:[{title:"How Silent Talk Works"},{name:"description",content:"Learn how camera capture, MediaPipe detection, classification, text, and speech work together."},{property:"og:title",content:"How Silent Talk Works"},{property:"og:description",content:"A transparent five-stage ASL recognition and communication pipeline."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:HowItWorks});
const steps=[[Camera,"Capture","Camera captures the user's hand gesture."],[Hand,"Detect","MediaPipe identifies hand landmarks."],[ScanLine,"Classify","A trained machine-learning model recognizes the ASL alphabet."],[MessageSquareText,"Display","The recognized character appears as text."],[Volume2,"Speak","Recognized text can be converted into audio."]] as const;
function HowItWorks(){return <div className="page-wrap"><PageIntro eyebrow="Transparent by design" title="How Silent Talk works" description="A focused path from visible gesture to clear communication, with each stage prepared for the existing recognition backend."/><div className="mt-12 grid gap-4 lg:grid-cols-5">{steps.map(([Icon,title,text],i)=><motion.article key={title} initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} transition={{delay:i*.08}} viewport={{once:true}} className="panel relative p-5"><span className="font-mono text-xs text-primary">0{i+1}</span><Icon className="mt-8 size-8 text-primary"/><h2 className="mt-5 font-display text-xl font-semibold uppercase">{title}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{text}</p>{i<4&&<span className="absolute -right-3 top-1/2 z-10 hidden text-2xl text-primary lg:block">→</span>}</motion.article>)}</div><section className="mt-16 border-t border-border pt-10"><SectionLabel>Technology</SectionLabel><div className="mt-5 flex flex-wrap gap-3">{["React","Python","MediaPipe","Scikit-Learn","OpenCV"].map(t=><span key={t} className="rounded-md border border-border bg-surface px-5 py-3 font-mono text-sm">{t}</span>)}</div></section></div>}